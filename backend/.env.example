# Example environment variables for Django backend
# Copy this file to .env and adjust as needed

# Django secret key (required)
SECRET_KEY = put your Django secret key here
# Allowed hosts for backend
ALLOWED_HOSTS=put your allowed hosts here, e.g., '127.0.0.1, localhost'
# DEBUG mode (True for development, False for production)
DEBUG=put True or False here